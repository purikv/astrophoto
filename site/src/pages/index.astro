---
import galleryData from '../data/gallery.json';
const base = import.meta.env.BASE_URL;
---
<html lang="uk">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Astro Gallery</title>
    <style>
      body {
        font-family: system-ui, -apple-system, sans-serif;
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
        background: #0a0a0a;
        color: #e0e0e0;
      }
      h1 { color: #fff; text-align: center; margin-bottom: 2rem; }
      .gallery {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(380px, 1fr));
        gap: 2rem;
      }
      .gallery-item {
        background: linear-gradient(145deg, #1e1e1e, #151515);
        border-radius: 12px;
        overflow: hidden;
        transition: all 0.3s ease;
        border: 1px solid #252525;
        box-shadow: 0 4px 6px rgba(0,0,0,0.3);
      }
      .gallery-item:hover {
        transform: translateY(-8px);
        box-shadow: 0 12px 24px rgba(74, 158, 255, 0.15);
        border-color: #4a9eff;
      }
      .gallery-item img {
        width: 100%;
        height: 280px;
        object-fit: cover;
        display: block;
        cursor: pointer;
        transition: transform 0.3s ease;
      }
      .gallery-item:hover img {
        transform: scale(1.05);
      }
      .gallery-info {
        padding: 1.5rem;
      }
      .object-header {
        margin-bottom: 1rem;
        border-bottom: 2px solid #2a2a2a;
        padding-bottom: 1rem;
      }
      .gallery-info h2 {
        margin: 0 0 0.5rem 0;
        font-size: 1.5rem;
        color: #fff;
        font-weight: 600;
      }
      .object-meta {
        display: flex;
        flex-wrap: wrap;
        gap: 0.75rem;
        margin-top: 0.75rem;
      }
      .meta-badge {
        background: #252525;
        padding: 0.35rem 0.75rem;
        border-radius: 20px;
        font-size: 0.75rem;
        color: #aaa;
        border: 1px solid #333;
      }
      .meta-badge strong {
        color: #4a9eff;
        font-weight: 600;
      }
      .description {
        font-style: italic;
        color: #999;
        margin: 1rem 0;
        font-size: 0.9rem;
        line-height: 1.6;
        padding: 0.75rem;
        background: #1a1a1a;
        border-left: 3px solid #4a9eff;
        border-radius: 4px;
      }
      .tech-details {
        margin-top: 1rem;
      }
      .tech-summary {
        cursor: pointer;
        user-select: none;
        font-size: 0.95rem;
        font-weight: 600;
        color: #4a9eff;
        padding: 0.75rem;
        background: #1a1a1a;
        border-radius: 6px;
        margin-bottom: 0.5rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        transition: background 0.2s;
      }
      .tech-summary:hover {
        background: #222;
      }
      .tech-summary::before {
        content: '▶';
        transition: transform 0.2s;
        font-size: 0.7rem;
      }
      details[open] .tech-summary::before {
        transform: rotate(90deg);
      }
      .tech-content {
        padding: 1rem;
        background: #181818;
        border-radius: 6px;
        margin-bottom: 1rem;
      }
      .tech-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 0.75rem;
        font-size: 0.85rem;
      }
      .tech-item {
        padding: 0.5rem;
        background: #1e1e1e;
        border-radius: 4px;
        border-left: 2px solid #333;
      }
      .tech-item-label {
        display: block;
        color: #888;
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 0.25rem;
      }
      .tech-item-value {
        color: #ddd;
        font-weight: 500;
      }
      .download-btn {
        display: block;
        margin-top: 1.25rem;
        padding: 0.75rem 1.5rem;
        background: linear-gradient(135deg, #4a9eff, #3a7fd5);
        color: white;
        border-radius: 8px;
        text-align: center;
        font-weight: 600;
        transition: all 0.3s;
        box-shadow: 0 2px 8px rgba(74, 158, 255, 0.3);
      }
      .download-btn:hover {
        background: linear-gradient(135deg, #5aa9ff, #4a8fe5);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(74, 158, 255, 0.5);
        text-decoration: none;
      }
      /* Lightbox styles */
      .lightbox {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.95);
        z-index: 9999;
        overflow: hidden;
      }
      .lightbox.active {
        display: flex;
        align-items: center;
        justify-content: center;
      }
      .lightbox-content {
        position: relative;
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
        touch-action: none;
      }
      .lightbox-image {
        max-width: 100%;
        max-height: 100%;
        object-fit: contain;
        transition: transform 0.2s ease-out;
        cursor: grab;
      }
      .lightbox-image.dragging {
        cursor: grabbing;
        transition: none;
      }
      .lightbox-close {
        position: fixed;
        top: 20px;
        right: 20px;
        background: rgba(255, 255, 255, 0.1);
        border: none;
        color: white;
        font-size: 32px;
        width: 50px;
        height: 50px;
        border-radius: 50%;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: background 0.2s;
        z-index: 10000;
      }
      .lightbox-close:hover {
        background: rgba(255, 255, 255, 0.2);
      }
      .lightbox-zoom-info {
        position: fixed;
        bottom: 20px;
        left: 50%;
        transform: translateX(-50%);
        background: rgba(0, 0, 0, 0.7);
        color: white;
        padding: 10px 20px;
        border-radius: 20px;
        font-size: 14px;
        pointer-events: none;
        opacity: 0;
        transition: opacity 0.3s;
      }
      .lightbox-zoom-info.show {
        opacity: 1;
      }
    </style>
  </head>
  <body>
    <h1>🌌 Astro Gallery</h1>
    <div class="gallery">
      {galleryData.map((item) => (
        <div class="gallery-item">
          {item.preview && <img src={`${base}/${item.preview}`} alt={item.title} loading="lazy" class="gallery-thumbnail" data-full={`${base}/${item.preview.replace('_800', '_2000')}`} />}

          <div class="gallery-info">
            <div class="object-header">
              <h2>{item.object?.name || item.title}</h2>

              <div class="object-meta">
                {item.object?.type && <span class="meta-badge"><strong>{item.object.type}</strong></span>}
                {item.object?.constellation && <span class="meta-badge">🌟 {item.object.constellation}</span>}
                {item.object?.distance_ly && (
                  <span class="meta-badge">📏 {item.object.distance_ly.toLocaleString('uk-UA')} св.р.</span>
                )}
                {item.session?.date_utc && <span class="meta-badge">📅 {item.session.date_utc}</span>}
                {item.session?.location && <span class="meta-badge">📍 {item.session.location}</span>}
              </div>
            </div>

            {item.object?.description && (
              <p class="description">{item.object.description}</p>
            )}

            <div class="tech-details">
              <details>
                <summary class="tech-summary">Обладнання та налаштування</summary>
                <div class="tech-content">
                  <div class="tech-grid">
                    {item.session?.camera && (
                      <div class="tech-item">
                        <span class="tech-item-label">Камера</span>
                        <div class="tech-item-value">{item.session.camera}</div>
                      </div>
                    )}
                    {item.session?.telescope && (
                      <div class="tech-item">
                        <span class="tech-item-label">Телескоп</span>
                        <div class="tech-item-value">{item.session.telescope}</div>
                      </div>
                    )}
                    {item.session?.mount && (
                      <div class="tech-item">
                        <span class="tech-item-label">Монтування</span>
                        <div class="tech-item-value">{item.session.mount}</div>
                      </div>
                    )}
                    {item.session?.filters && item.session.filters.length > 0 && (
                      <div class="tech-item">
                        <span class="tech-item-label">Фільтри</span>
                        <div class="tech-item-value">{item.session.filters.join(', ')}</div>
                      </div>
                    )}
                    {item.session?.guiding && (
                      <div class="tech-item">
                        <span class="tech-item-label">Гідування</span>
                        <div class="tech-item-value">RMS {item.session.guiding.rms_arcsec}" ({item.session.guiding.software})</div>
                      </div>
                    )}
                    {item.session?.bortle && (
                      <div class="tech-item">
                        <span class="tech-item-label">Bortle Scale</span>
                        <div class="tech-item-value">{item.session.bortle}</div>
                      </div>
                    )}
                  </div>
                </div>
              </details>

              {item.session?.subs && item.session.subs.length > 0 && (
                <details>
                  <summary class="tech-summary">Експозиції та калібрація</summary>
                  <div class="tech-content">
                    <div class="tech-grid">
                      {item.session.subs.map((sub, idx) => (
                        <div class="tech-item" key={idx}>
                          <span class="tech-item-label">{sub.band}</span>
                          <div class="tech-item-value">
                            {sub.count} × {sub.exposure_s}s
                            {sub.iso && ` (ISO ${sub.iso})`}
                          </div>
                        </div>
                      ))}
                      <div class="tech-item">
                        <span class="tech-item-label">Загальний час</span>
                        <div class="tech-item-value">
                          {Math.floor(item.session.subs.reduce((acc, sub) => acc + (sub.count * sub.exposure_s), 0) / 60)} хв
                        </div>
                      </div>
                      {item.session.calibration?.bias && (
                        <div class="tech-item">
                          <span class="tech-item-label">Bias</span>
                          <div class="tech-item-value">{item.session.calibration.bias}</div>
                        </div>
                      )}
                      {item.session.calibration?.darks && (
                        <div class="tech-item">
                          <span class="tech-item-label">Darks</span>
                          <div class="tech-item-value">{item.session.calibration.darks.count} × {item.session.calibration.darks.exposure_s}s</div>
                        </div>
                      )}
                      {item.session.calibration?.flats && (
                        <div class="tech-item">
                          <span class="tech-item-label">Flats</span>
                          <div class="tech-item-value">{item.session.calibration.flats.count}</div>
                        </div>
                      )}
                    </div>
                  </div>
                </details>
              )}

              {(item.session?.stacking_software || item.session?.processing_software) && (
                <details>
                  <summary class="tech-summary">Обробка</summary>
                  <div class="tech-content">
                    <div class="tech-grid">
                      {item.session.stacking_software && (
                        <div class="tech-item">
                          <span class="tech-item-label">Стекінг</span>
                          <div class="tech-item-value">{item.session.stacking_software}</div>
                        </div>
                      )}
                      {item.session.processing_software && item.session.processing_software.length > 0 && (
                        <div class="tech-item">
                          <span class="tech-item-label">Обробка</span>
                          <div class="tech-item-value">{item.session.processing_software.join(', ')}</div>
                        </div>
                      )}
                    </div>
                    {item.session.notes && (
                      <div style="margin-top: 0.75rem; font-size: 0.85rem; color: #999; font-style: italic;">
                        {item.session.notes}
                      </div>
                    )}
                  </div>
                </details>
              )}
            </div>

            {item.final && (
              <a href={`${base}/${item.final}`} download class="download-btn">
                ⬇️ Завантажити високу якість
              </a>
            )}
          </div>
        </div>
      ))}
    </div>

    <!-- Lightbox Modal -->
    <div class="lightbox" id="lightbox">
      <button class="lightbox-close" id="lightbox-close">&times;</button>
      <div class="lightbox-content" id="lightbox-content">
        <img src="" alt="" class="lightbox-image" id="lightbox-image" />
      </div>
      <div class="lightbox-zoom-info" id="zoom-info">Колесико миші для zoom</div>
    </div>

    <script>
      // Lightbox functionality with zoom and pan
      const lightbox = document.getElementById('lightbox');
      const lightboxImage = document.getElementById('lightbox-image');
      const lightboxContent = document.getElementById('lightbox-content');
      const lightboxClose = document.getElementById('lightbox-close');
      const zoomInfo = document.getElementById('zoom-info');
      const thumbnails = document.querySelectorAll('.gallery-thumbnail');

      let scale = 1;
      let translateX = 0;
      let translateY = 0;
      let isDragging = false;
      let startX = 0;
      let startY = 0;
      let lastTouchDistance = 0;

      // Open lightbox
      thumbnails.forEach(thumb => {
        thumb.addEventListener('click', (e) => {
          const fullSrc = e.target.dataset.full;
          const alt = e.target.alt;

          lightboxImage.src = fullSrc;
          lightboxImage.alt = alt;
          lightbox.classList.add('active');
          document.body.style.overflow = 'hidden';

          // Show zoom info briefly
          zoomInfo.classList.add('show');
          setTimeout(() => zoomInfo.classList.remove('show'), 2000);

          // Reset transform
          scale = 1;
          translateX = 0;
          translateY = 0;
          updateTransform();
        });
      });

      // Close lightbox
      const closeLightbox = () => {
        lightbox.classList.remove('active');
        document.body.style.overflow = '';
        scale = 1;
        translateX = 0;
        translateY = 0;
        updateTransform();
      };

      lightboxClose.addEventListener('click', closeLightbox);

      lightbox.addEventListener('click', (e) => {
        if (e.target === lightbox) {
          closeLightbox();
        }
      });

      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && lightbox.classList.contains('active')) {
          closeLightbox();
        }
      });

      // Update transform
      const updateTransform = () => {
        lightboxImage.style.transform = `translate(${translateX}px, ${translateY}px) scale(${scale})`;
      };

      // Mouse wheel zoom
      lightboxContent.addEventListener('wheel', (e) => {
        e.preventDefault();

        const delta = e.deltaY > 0 ? -0.1 : 0.1;
        const newScale = Math.max(1, Math.min(5, scale + delta));

        if (newScale !== scale) {
          scale = newScale;

          // Reset position when zooming out to 1
          if (scale === 1) {
            translateX = 0;
            translateY = 0;
          }

          updateTransform();
        }
      });

      // Mouse drag to pan
      lightboxImage.addEventListener('mousedown', (e) => {
        if (scale > 1) {
          isDragging = true;
          startX = e.clientX - translateX;
          startY = e.clientY - translateY;
          lightboxImage.classList.add('dragging');
        }
      });

      document.addEventListener('mousemove', (e) => {
        if (isDragging) {
          translateX = e.clientX - startX;
          translateY = e.clientY - startY;
          updateTransform();
        }
      });

      document.addEventListener('mouseup', () => {
        isDragging = false;
        lightboxImage.classList.remove('dragging');
      });

      // Touch pinch-to-zoom
      lightboxContent.addEventListener('touchstart', (e) => {
        if (e.touches.length === 2) {
          e.preventDefault();
          const touch1 = e.touches[0];
          const touch2 = e.touches[1];
          lastTouchDistance = Math.hypot(
            touch2.clientX - touch1.clientX,
            touch2.clientY - touch1.clientY
          );
        } else if (e.touches.length === 1 && scale > 1) {
          isDragging = true;
          startX = e.touches[0].clientX - translateX;
          startY = e.touches[0].clientY - translateY;
        }
      }, { passive: false });

      lightboxContent.addEventListener('touchmove', (e) => {
        if (e.touches.length === 2) {
          e.preventDefault();
          const touch1 = e.touches[0];
          const touch2 = e.touches[1];
          const currentDistance = Math.hypot(
            touch2.clientX - touch1.clientX,
            touch2.clientY - touch1.clientY
          );

          if (lastTouchDistance > 0) {
            const delta = (currentDistance - lastTouchDistance) * 0.01;
            const newScale = Math.max(1, Math.min(5, scale + delta));

            if (newScale !== scale) {
              scale = newScale;

              if (scale === 1) {
                translateX = 0;
                translateY = 0;
              }

              updateTransform();
            }
          }

          lastTouchDistance = currentDistance;
        } else if (e.touches.length === 1 && isDragging) {
          e.preventDefault();
          translateX = e.touches[0].clientX - startX;
          translateY = e.touches[0].clientY - startY;
          updateTransform();
        }
      }, { passive: false });

      lightboxContent.addEventListener('touchend', (e) => {
        if (e.touches.length === 0) {
          isDragging = false;
          lastTouchDistance = 0;
        }
      });
    </script>
  </body>
</html>
