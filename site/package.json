{
  "name": "astro-gallery",
  "private": true,
  "scripts": {
    "generate": "node ../tools/generate-gallery-json.mjs",
    "dev": "astro dev",
    "build": "npm run generate && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.0.0",
    "gray-matter": "^4.0.3",
    "yaml": "^2.4.2"
  }
}