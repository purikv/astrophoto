# Швидкий старт: Додавання нового астрофото

## Крок 1: Підготовка зображення

Покладіть ваше фінальне зображення у відповідну директорію:

```bash
images/<object-id>/final/your-image.png
```

Приклад:
```bash
images/m33/final/M33_Triangulum.png
```

## Крок 2: Запуск інтерактивного інструменту

```bash
npm run add
```

або

```bash
node tools/add-gallery-item.mjs
```

## Крок 3: Відповідайте на питання

Інструмент проведе вас через всі необхідні кроки:

### Створення об'єкта

1. **ID об'єкта**: Унікальний ідентифікатор (напр.: `m33`, `ngc281`)
   - Малі літери, латиниця
   - Без пробілів, тільки дефіси для множинних об'єктів

2. **Українська назва**: "Галактика Трикутника"

3. **Тип об'єкта**: Галактика, Туманність, Скупчення тощо

4. **Сузір'я**: "Трикутник", "Оріон", "Кассіопея" тощо

5. **Альтернативні назви** (через кому): Messier 33, M33, NGC 598

6. **Відстань** (світлові роки): 2730000 або Enter для пропуску

7. **Опис**: Короткий цікавий опис об'єкта

8. **Розширений опис**: Додаткова наукова інформація (опціонально)

### Створення сесії зйомки

1. **Дата зйомки** (YYYY-MM-DD): 2024-11-24

2. **Місце**: Dnipropetrovska oblast, UA

3. **Bortle** (1-9): Шкала світлового забруднення

4. **Обладнання**:
   - Камера
   - Телескоп
   - Монтування
   - Фільтри

5. **Гідування**: Так/Ні + параметри якщо так

6. **Експозиції**:
   - Діапазон (Luminance, RGB, Ha тощо)
   - ISO
   - Експозиція (секунди)
   - Кількість кадрів

7. **Калібрувальні кадри**:
   - Bias
   - Darks
   - Flats

8. **Обробка**:
   - Софт для стекінгу (Siril, PixInsight тощо)
   - Софт для обробки

9. **Шляхи до файлів**:
   - Фінальне зображення: `images/m33/final/M33.png`
   - Превью: `thumbnails/m33_800.jpg` (генерується автоматично)

## Крок 4: Коміт і push

```bash
git add data/ images/
git commit -m "Додано M33 Галактика Трикутника"
git push
```

**Готово!** 🎉 GitHub Actions автоматично:
- ✅ Перевірить валідність YAML файлів
- ✅ Згенерує thumbnails (800px та 2000px)
- ✅ Створить gallery.json
- ✅ Збере та задеплоїть сайт на GitHub Pages

Якщо валідація не пройде, деплой буде скасовано і ви побачите помилку в GitHub Actions.

---

## Опціональні кроки (для локальної розробки)

### Генерація превью локально

```bash
npm run thumbs
```

Створить оптимізовані превью у двох розмірах:
- 800px ширина (для галереї)
- 2000px ширина (для детального перегляду)

### Перевірка локально

```bash
cd site
npm run dev
```

Відкрийте http://localhost:4321/astrophoto у браузері

### Валідація перед комітом

```bash
npm run validate
```

Перевірить правильність всіх YAML файлів (виконується автоматично в CI)

## Поширені питання

### Як додати кілька наборів експозицій?

При створенні сесії інструмент запитає "Додати ще один набір кадрів?". Відповідайте "y" для додавання наступних наборів (наприклад, Ha + OIII + SII для SHO зображення).

### Що якщо я зробив помилку?

1. Запустіть інструмент знову
2. Коли він запитає про перезапис файлу, відповідайте "y"
3. Або відредагуйте YAML файл вручну в текстовому редакторі

### Як створити тільки сесію для існуючого об'єкта?

1. Запустіть `npm run add`
2. На питання "Створити новий об'єкт?" відповідайте "n"
3. Введіть ID існуючого об'єкта
4. Продовжуйте з створенням сесії

### Де знайти шаблони для ручного редагування?

```
data/templates/object-template.yml
data/templates/session-template.yml
```

## Підказки

- **Використовуйте Tab** для автодоповнення в термінал
- **Enter** приймає значення за замовчуванням (показані жовтим)
- **Ctrl+C** для виходу в будь-який момент
- Всі поля з **дефолтними значеннями** можна просто підтвердити Enter
- **Перевіряйте формат дат**: рік-місяць-день (YYYY-MM-DD)

## Корисні посилання

- [Детальна документація](data/README.md)
- [Messier каталог](https://en.wikipedia.org/wiki/Messier_object)
- [NGC/IC каталог](https://en.wikipedia.org/wiki/New_General_Catalogue)
- [Bortle шкала](https://en.wikipedia.org/wiki/Bortle_scale)
